# Signal index API key. Server-side only — never exposed to the browser.
# For a Vercel deployment set it there instead: vercel env add SENTINEL_API_KEY
SENTINEL_API_KEY=

# Optional override for the upstream base URL
SENTINEL_API_BASE_URL=
